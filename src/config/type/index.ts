export type AlarmTime = '5분 전' | '10분 전' | '30분 전' | '1시간 전';

export type SalesStatus = '판매중' | '예약중' | '판매완료' | '삭제됨';

export enum PetType {
  'dog' = '강아지',
  'cat' = '고양이',
}

export enum PetGender {
  'M' = '남아',
  'F' = '여아',
}
